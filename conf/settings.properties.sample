# Format:
# key=value
# Empty lines are ignored, anything after a '#' is ignored too

# URL= Redmine site (url)
URL=

# KEY= redmine api key (alfanumeric string)
KEY=

# READ_ONLY= set to true to skip PUT/POST petitions (they will be logged)
READ_ONLY=
